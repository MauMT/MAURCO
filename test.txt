program Ejemplo;

class Libro{
attributes<
  vars 
  int pags;
>

methods
<
  int func byeWorld()
  vars
  int send;
  {
  print(send)
    return (send);
  }
  
  void func nowo()
  vars
  int mail;
  {
  print(mail) 
    return;
  }
>

}

vars
{
    object karen claro;
    int i;
    float valor;
}

void func helloWorld()
vars
int msg;

{
print(msg)
  return;
}

main(){ 
    a = (x+5);
    y = 5 * 2;
    helloWorld();
    x = x/3;

    if(x < 3 & x+9 != y) then{
      print(2)
    }
    else{
      print(3.5)
    }
    print(x+3)
    input(x,y);
    input(z);
    z=suma(1,5);
    while(x<4 | y<5) do{
      x = (y+1);
      libro.pags = (libro.pags+1);
    }
    x = (y+5*3);
}
end





ELIMINE EL FROM
    from x=0 to 10 do{
      print(3)
      y = y+5;
      b = b +y+1;
    }
